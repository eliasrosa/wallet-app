export enum TickerTypeColor {
	ETF = 'hsl(var(--chart-2))',
	FII = 'hsl(var(--chart-1))',
	STOCK = 'hsl(var(--chart-5))',
	RF = 'hsl(var(--chart-3))',
}

export enum TickerTypeLabel {
	ETF = 'ETF',
	STOCK = 'Ações',
	RF = 'Renda Fixa',
	FII = 'Fundos Imobiliários',
}
